<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.arcguard.firesafety.facility.mapper.PanelMapper">
    <resultMap id="panelResultMap" type="com.arcguard.firesafety.facility.model.Panel">
        <id property="panelId" column="panel_id"/>
        <result property="siteId" column="site_id"/>
        <result property="name" column="name"/>
        <result property="deviceSerial" column="device_serial"/>
        <result property="mNo" column="m_no"/>
        <result property="installedAt" column="installed_at"/>
        <result property="status" column="status"/>
        <result property="isOnline" column="is_online"/>
        <result property="lastCommunicatedAt" column="last_communicated_at"/>
        <result property="circuitCount" column="circuit_count"/>
        <result property="leakMaThreshold" column="leak_ma_threshold"/>
        <result property="tempThreshold" column="temp_threshold"/>
        <result property="humidityThreshold" column="humidity_threshold"/>
        <result property="overcurrentThreshold" column="overcurrent_threshold"/>
        <result property="gasThreshold" column="gas_threshold"/>
        <result property="fireThreshold" column="fire_threshold"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="deletedAt" column="deleted_at"/>
    </resultMap>

    <!-- 현장 아래 신규 분전반 등록 -->
    <insert id="insertPanel"
            parameterType="com.arcguard.firesafety.facility.model.Panel"
            useGeneratedKeys="true"
            keyProperty="panelId">
        INSERT INTO panel (
            site_id,
            name,
            device_serial,
            m_no,
            installed_at,
            status,
            is_online,
            circuit_count,
            leak_ma_threshold,
            temp_threshold,
            humidity_threshold,
            overcurrent_threshold,
            gas_threshold,
            fire_threshold
        ) VALUES (
            #{siteId},
            #{name},
            #{deviceSerial},
            #{mNo},
            #{installedAt},
            #{status},
            #{isOnline},
            #{circuitCount},
            #{leakMaThreshold},
            #{tempThreshold},
            #{humidityThreshold},
            #{overcurrentThreshold},
            #{gasThreshold},
            #{fireThreshold}
        )
    </insert>

    <!-- 등록/수정/삭제 대상 분전반 활성 여부 확인 -->
    <select id="findActivePanelById" resultMap="panelResultMap">
        SELECT
            p.panel_id,
            p.site_id,
            p.name,
            p.device_serial,
            p.m_no,
            p.installed_at,
            p.status,
            p.is_online,
            p.last_communicated_at,
            p.circuit_count,
            p.leak_ma_threshold,
            p.temp_threshold,
            p.humidity_threshold,
            p.overcurrent_threshold,
            p.gas_threshold,
            p.fire_threshold,
            p.created_at,
            p.updated_at,
            p.deleted_at
        FROM panel p
        INNER JOIN site s ON s.site_id = p.site_id
        WHERE p.panel_id = #{panelId}
          AND p.deleted_at IS NULL
          AND s.deleted_at IS NULL
    </select>

    <!-- 디바이스가 보낸 m_no로 활성 분전반 확인 -->
    <select id="findActivePanelByMNo" resultMap="panelResultMap">
        SELECT
            p.panel_id,
            p.site_id,
            p.name,
            p.device_serial,
            p.m_no,
            p.installed_at,
            p.status,
            p.is_online,
            p.last_communicated_at,
            p.circuit_count,
            p.leak_ma_threshold,
            p.temp_threshold,
            p.humidity_threshold,
            p.overcurrent_threshold,
            p.gas_threshold,
            p.fire_threshold,
            p.created_at,
            p.updated_at,
            p.deleted_at
        FROM panel p
        INNER JOIN site s ON s.site_id = p.site_id
        WHERE p.m_no = #{mNo}
          AND p.deleted_at IS NULL
          AND s.deleted_at IS NULL
    </select>

    <!-- 현장 상세에 표시할 활성 분전반 수 -->
    <select id="countActivePanelsBySiteId" resultType="int">
        SELECT COUNT(*)
        FROM panel p
        INNER JOIN site s ON s.site_id = p.site_id
        WHERE p.site_id = #{siteId}
          AND p.deleted_at IS NULL
          AND s.deleted_at IS NULL
    </select>

    <!-- 전체 활성 분전반 조회(필터/페이징 없음) — Mock 센서/AI 스케줄러 등 내부 배치용 -->
    <select id="findAllActivePanels" resultMap="panelResultMap">
        SELECT
            p.panel_id,
            p.site_id,
            p.name,
            p.device_serial,
            p.m_no,
            p.installed_at,
            p.status,
            p.is_online,
            p.last_communicated_at,
            p.circuit_count,
            p.leak_ma_threshold,
            p.temp_threshold,
            p.humidity_threshold,
            p.overcurrent_threshold,
            p.gas_threshold,
            p.fire_threshold,
            p.created_at,
            p.updated_at,
            p.deleted_at
        FROM panel p
        INNER JOIN site s ON s.site_id = p.site_id
        WHERE p.deleted_at IS NULL
          AND s.deleted_at IS NULL
        ORDER BY p.panel_id DESC
    </select>

    <!-- SUPER_ADMIN은 전체 활성 분전반 조회(화면 목록 API, 필터/페이징 지원) -->
    <select id="findActivePanels" resultMap="panelResultMap">
        SELECT
            p.panel_id,
            p.site_id,
            p.name,
            p.device_serial,
            p.m_no,
            p.installed_at,
            p.status,
            p.is_online,
            p.last_communicated_at,
            p.circuit_count,
            p.leak_ma_threshold,
            p.temp_threshold,
            p.humidity_threshold,
            p.overcurrent_threshold,
            p.gas_threshold,
            p.fire_threshold,
            p.created_at,
            p.updated_at,
            p.deleted_at
        FROM panel p
        INNER JOIN site s ON s.site_id = p.site_id
        WHERE p.deleted_at IS NULL
          AND s.deleted_at IS NULL
        <if test="siteId != null">
          AND p.site_id = #{siteId}
        </if>
        <if test="status != null">
          AND p.status = #{status}
        </if>
        <if test="keyword != null and keyword != ''">
          AND (
            p.name LIKE CONCAT('%', #{keyword}, '%')
            OR p.device_serial LIKE CONCAT('%', #{keyword}, '%')
            OR p.m_no LIKE CONCAT('%', #{keyword}, '%')
          )
        </if>
        ORDER BY p.panel_id DESC
        LIMIT #{size}
        OFFSET #{offset}
    </select>

    <!-- SUPER_ADMIN 분전반 목록 전체 개수 -->
    <select id="countActivePanels" resultType="long">
        SELECT COUNT(*)
        FROM panel p
        INNER JOIN site s ON s.site_id = p.site_id
        WHERE p.deleted_at IS NULL
          AND s.deleted_at IS NULL
        <if test="siteId != null">
          AND p.site_id = #{siteId}
        </if>
        <if test="status != null">
          AND p.status = #{status}
        </if>
        <if test="keyword != null and keyword != ''">
          AND (
            p.name LIKE CONCAT('%', #{keyword}, '%')
            OR p.device_serial LIKE CONCAT('%', #{keyword}, '%')
            OR p.m_no LIKE CONCAT('%', #{keyword}, '%')
          )
        </if>
    </select>

    <!-- ADMIN/GENERAL은 배정 현장의 활성 분전반만 조회 -->
    <select id="findActivePanelsByUserId" resultMap="panelResultMap">
        SELECT
            p.panel_id,
            p.site_id,
            p.name,
            p.device_serial,
            p.m_no,
            p.installed_at,
            p.status,
            p.is_online,
            p.last_communicated_at,
            p.circuit_count,
            p.leak_ma_threshold,
            p.temp_threshold,
            p.humidity_threshold,
            p.overcurrent_threshold,
            p.gas_threshold,
            p.fire_threshold,
            p.created_at,
            p.updated_at,
            p.deleted_at
        FROM panel p
        INNER JOIN site s ON s.site_id = p.site_id
        INNER JOIN user_site us ON us.site_id = s.site_id
        WHERE us.user_id = #{userId}
          AND us.deleted_at IS NULL
          AND p.deleted_at IS NULL
          AND s.deleted_at IS NULL
        <if test="siteId != null">
          AND p.site_id = #{siteId}
        </if>
        <if test="status != null">
          AND p.status = #{status}
        </if>
        <if test="keyword != null and keyword != ''">
          AND (
            p.name LIKE CONCAT('%', #{keyword}, '%')
            OR p.device_serial LIKE CONCAT('%', #{keyword}, '%')
            OR p.m_no LIKE CONCAT('%', #{keyword}, '%')
          )
        </if>
        ORDER BY p.panel_id DESC
        LIMIT #{size}
        OFFSET #{offset}
    </select>

    <!-- ADMIN/GENERAL 담당 현장 분전반 목록 전체 개수 -->
    <select id="countActivePanelsByUserId" resultType="long">
        SELECT COUNT(*)
        FROM panel p
        INNER JOIN site s ON s.site_id = p.site_id
        INNER JOIN user_site us ON us.site_id = s.site_id
        WHERE us.user_id = #{userId}
          AND us.deleted_at IS NULL
          AND p.deleted_at IS NULL
          AND s.deleted_at IS NULL
        <if test="siteId != null">
          AND p.site_id = #{siteId}
        </if>
        <if test="status != null">
          AND p.status = #{status}
        </if>
        <if test="keyword != null and keyword != ''">
          AND (
            p.name LIKE CONCAT('%', #{keyword}, '%')
            OR p.device_serial LIKE CONCAT('%', #{keyword}, '%')
            OR p.m_no LIKE CONCAT('%', #{keyword}, '%')
          )
        </if>
    </select>

    <!-- 장비 시리얼 중복 등록 방지 -->
    <select id="existsPanelByDeviceSerial" resultType="boolean">
        SELECT EXISTS(
            SELECT 1
            FROM panel
            WHERE device_serial = #{deviceSerial}
        )
    </select>

    <!-- 수정 시 자기 자신을 제외한 장비 시리얼 중복 방지 -->
    <select id="existsPanelByDeviceSerialExceptSelf" resultType="boolean">
        SELECT EXISTS(
            SELECT 1
            FROM panel
            WHERE device_serial = #{deviceSerial}
              AND panel_id != #{panelId}
        )
    </select>

    <!-- 장비 번호 중복 등록 방지. m_no는 DB 제약이 없어 삭제된 분전반의 값은 검사에서 제외(재사용 허용) -->
    <select id="existsPanelByMNo" resultType="boolean">
        SELECT EXISTS(
            SELECT 1
            FROM panel
            WHERE m_no = #{mNo}
              AND deleted_at IS NULL
        )
    </select>

    <!-- 수정 시 자기 자신을 제외한 장비 번호 중복 방지 -->
    <select id="existsPanelByMNoExceptSelf" resultType="boolean">
        SELECT EXISTS(
            SELECT 1
            FROM panel
            WHERE m_no = #{mNo}
              AND panel_id != #{panelId}
              AND deleted_at IS NULL
        )
    </select>

    <!-- 관리자 분전반 기본 정보와 임계치 수정 -->
    <update id="updatePanel" parameterType="com.arcguard.firesafety.facility.model.Panel">
        UPDATE panel
        SET name = #{name},
            device_serial = #{deviceSerial},
            m_no = #{mNo},
            installed_at = #{installedAt},
            circuit_count = #{circuitCount},
            leak_ma_threshold = #{leakMaThreshold},
            temp_threshold = #{tempThreshold},
            humidity_threshold = #{humidityThreshold},
            overcurrent_threshold = #{overcurrentThreshold},
            gas_threshold = #{gasThreshold},
            fire_threshold = #{fireThreshold},
            updated_at = CURRENT_TIMESTAMP
        WHERE panel_id = #{panelId}
          AND deleted_at IS NULL
    </update>

    <!-- 분전반 row는 보존하고 일반 조회 대상에서 제외. device_serial은 DB UNIQUE 제약이 있어
         값을 그대로 두면 같은 시리얼로 재등록이 영구히 막힌다 — panel_id를 붙여 값을 바꿔 우회한다.
         name도 같은 규칙으로 바꿔 목록/이력에서 삭제된 분전반임을 구분할 수 있게 한다.
         감사 로그(facility_audit_log)의 DELETE 액션에는 변형 전 원본 값이 그대로 남는다(메모리 객체 기준). -->
    <update id="softDeletePanel">
        UPDATE panel
        SET deleted_at = CURRENT_TIMESTAMP,
            updated_at = CURRENT_TIMESTAMP,
            device_serial = CONCAT(LEFT(device_serial, 35), '_DEL_', panel_id),
            name = CONCAT(LEFT(name, 85), '_DEL_', panel_id)
        WHERE panel_id = #{panelId}
          AND deleted_at IS NULL
    </update>

    <!-- 정상 수신된 분전반을 온라인 상태로 갱신 -->
    <update id="updatePanelCommunication">
        UPDATE panel
        SET is_online = 1,
            last_communicated_at = CURRENT_TIMESTAMP,
            updated_at = CURRENT_TIMESTAMP
        WHERE panel_id = #{panelId}
          AND deleted_at IS NULL
    </update>

    <resultMap id="sensorFrameResultMap" type="com.arcguard.firesafety.sensor.model.SensorFrame">
        <id property="frameId" column="frame_id"/>
        <result property="panelId" column="panel_id"/>
        <result property="receivedAt" column="received_at"/>
        <result property="mode" column="mode"/>
        <result property="voltV" column="volt_v"/>
        <result property="leakMa" column="leak_ma"/>
        <result property="temperature" column="temperature"/>
        <result property="humidity" column="humidity"/>
        <result property="fireRaw" column="fire_raw"/>
        <result property="gasRaw" column="gas_raw"/>
        <result property="errorBits" column="error_bits"/>
        <result property="doorStatus" column="door_status"/>
        <result property="totalCurrent" column="total_current"/>
        <result property="totalPower" column="total_power"/>
    </resultMap>

    <!-- 분전반 상세용 최신 센서 프레임 조회 -->
    <select id="findLatestSensorFrameByPanelId" resultMap="sensorFrameResultMap">
        SELECT
            sf.frame_id,
            sf.panel_id,
            sf.received_at,
            sf.mode,
            sf.volt_v,
            sf.leak_ma,
            sf.temperature,
            sf.humidity,
            sf.fire_raw,
            sf.gas_raw,
            sf.error_bits,
            sf.door_status,
            sf.total_current,
            sf.total_power
        FROM sensor_frame sf
        WHERE sf.panel_id = #{panelId}
        ORDER BY sf.frame_id DESC
        LIMIT 1
    </select>

    <resultMap id="circuitStatusRowResultMap" type="com.arcguard.firesafety.facility.model.CircuitStatusRow">
        <id property="circuitId" column="circuit_id"/>
        <result property="channelNo" column="channel_no"/>
        <result property="loadType" column="load_type"/>
        <result property="currentA" column="current_a"/>
        <result property="arcCounter" column="arc_counter"/>
        <result property="deviceArcFlag" column="device_arc_flag"/>
        <result property="latestAiVerdict" column="latest_ai_verdict"/>
    </resultMap>

    <!-- 분전반 상세용 회로별 최신 전류/아크/AI 판정 원시값 조회 -->
    <select id="findCircuitStatusRowsByPanelId" resultMap="circuitStatusRowResultMap">
        SELECT
            c.circuit_id,
            c.channel_no,
            c.load_type,
            latest_sfc.current_a,
            latest_sfc.arc_counter,
            latest_sfc.device_arc_flag,
            latest_ai.verdict AS latest_ai_verdict
        FROM circuit c
        LEFT JOIN sensor_frame_circuit latest_sfc
            ON latest_sfc.reading_id = (
                SELECT MAX(sfc.reading_id)
                FROM sensor_frame_circuit sfc
                WHERE sfc.circuit_id = c.circuit_id
            )
        LEFT JOIN ai_diagnosis_result latest_ai
            ON latest_ai.result_id = (
                SELECT MAX(ai.result_id)
                FROM ai_diagnosis_result ai
                WHERE ai.circuit_id = c.circuit_id
            )
        WHERE c.panel_id = #{panelId}
          AND c.deleted_at IS NULL
        ORDER BY c.channel_no ASC
    </select>

    <resultMap id="panelRecentAlertResultMap" type="com.arcguard.firesafety.facility.dto.res.PanelRecentAlertRes">
        <id property="alertId" column="alert_id"/>
        <result property="type" column="type"/>
        <result property="triggeredAt" column="triggered_at"/>
    </resultMap>

    <!-- 분전반 상세용 최근 경보 조회 (최신순) -->
    <select id="findRecentAlertsByPanelId" resultMap="panelRecentAlertResultMap">
        SELECT
            a.alert_id,
            a.type,
            a.triggered_at
        FROM alert a
        WHERE a.panel_id = #{panelId}
        ORDER BY a.triggered_at DESC, a.alert_id DESC
        LIMIT #{limit}
    </select>
</mapper>
