<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.arcguard.firesafety.monitoring.mapper.PanelStatusAggregationMapper">
    <resultMap id="circuitStatusSnapshotResultMap" type="com.arcguard.firesafety.monitoring.model.CircuitStatusSnapshot">
        <id property="circuitId" column="circuit_id"/>
        <result property="channelNo" column="channel_no"/>
        <result property="deviceArcFlag" column="device_arc_flag"/>
        <result property="latestAiVerdict" column="latest_ai_verdict"/>
    </resultMap>

    <!-- 분전반의 가장 최근 수신 프레임 error_bits 조회 -->
    <select id="findLatestErrorBitsByPanelId" resultType="string">
        SELECT sf.error_bits
        FROM sensor_frame sf
        WHERE sf.panel_id = #{panelId}
        ORDER BY sf.frame_id DESC
        LIMIT 1
    </select>

    <select id="findCurrentPanelStatus" resultType="string">
        SELECT p.status
        FROM panel p
        WHERE p.panel_id = #{panelId}
          AND p.deleted_at IS NULL
    </select>

    <!-- 분전반의 가장 최근 수신 프레임 door_status 조회 -->
    <select id="findLatestDoorStatusByPanelId" resultType="boolean">
        SELECT sf.door_status
        FROM sensor_frame sf
        WHERE sf.panel_id = #{panelId}
        ORDER BY sf.frame_id DESC
        LIMIT 1
    </select>

    <!-- 회로별 최신 하드웨어 ARC flag와 최신 AI 판정 조회 -->
    <select id="findCircuitStatusSnapshots" resultMap="circuitStatusSnapshotResultMap">
        SELECT
            c.circuit_id,
            c.channel_no,
            latest_sfc.device_arc_flag,
            latest_ai.verdict AS latest_ai_verdict
        FROM circuit c
        LEFT JOIN sensor_frame_circuit latest_sfc
            ON latest_sfc.reading_id = (
                SELECT MAX(sfc.reading_id)
                FROM sensor_frame_circuit sfc
                WHERE sfc.circuit_id = c.circuit_id
            )
        LEFT JOIN ai_diagnosis_result latest_ai
            ON latest_ai.result_id = (
                SELECT MAX(ai.result_id)
                FROM ai_diagnosis_result ai
                WHERE ai.circuit_id = c.circuit_id
            )
        WHERE c.panel_id = #{panelId}
          AND c.deleted_at IS NULL
        ORDER BY c.channel_no ASC
    </select>

    <!-- 분전반 서버 주의 기준값이 30초 이상 지속되는지 확인 -->
    <select id="hasSustainedThresholdCaution" resultType="boolean">
        SELECT EXISTS(
            SELECT 1
            FROM panel p
            WHERE p.panel_id = #{panelId}
              AND p.deleted_at IS NULL
              AND (
                  EXISTS (
                      SELECT 1
                      FROM sensor_frame start_sf
                      WHERE start_sf.panel_id = p.panel_id
                        AND start_sf.received_at &lt;= #{thresholdAt}
                        AND start_sf.leak_ma &gt;= p.leak_ma_threshold
                        AND NOT EXISTS (
                            SELECT 1
                            FROM sensor_frame recover_sf
                            WHERE recover_sf.panel_id = p.panel_id
                              AND recover_sf.received_at &gt; start_sf.received_at
                              AND recover_sf.leak_ma &lt; p.leak_ma_threshold
                        )
                  )
                  OR EXISTS (
                      SELECT 1
                      FROM sensor_frame start_sf
                      WHERE start_sf.panel_id = p.panel_id
                        AND start_sf.received_at &lt;= #{thresholdAt}
                        AND start_sf.temperature &gt;= p.temp_threshold
                        AND NOT EXISTS (
                            SELECT 1
                            FROM sensor_frame recover_sf
                            WHERE recover_sf.panel_id = p.panel_id
                              AND recover_sf.received_at &gt; start_sf.received_at
                              AND recover_sf.temperature &lt; p.temp_threshold
                        )
                  )
                  OR EXISTS (
                      SELECT 1
                      FROM sensor_frame start_sf
                      WHERE start_sf.panel_id = p.panel_id
                        AND start_sf.received_at &lt;= #{thresholdAt}
                        AND start_sf.humidity &gt;= p.humidity_threshold
                        AND NOT EXISTS (
                            SELECT 1
                            FROM sensor_frame recover_sf
                            WHERE recover_sf.panel_id = p.panel_id
                              AND recover_sf.received_at &gt; start_sf.received_at
                              AND recover_sf.humidity &lt; p.humidity_threshold
                        )
                  )
                  OR EXISTS (
                      SELECT 1
                      FROM sensor_frame start_sf
                      WHERE start_sf.panel_id = p.panel_id
                        AND start_sf.received_at &lt;= #{thresholdAt}
                        AND start_sf.total_current &gt;= p.overcurrent_threshold
                        AND NOT EXISTS (
                            SELECT 1
                            FROM sensor_frame recover_sf
                            WHERE recover_sf.panel_id = p.panel_id
                              AND recover_sf.received_at &gt; start_sf.received_at
                              AND recover_sf.total_current &lt; p.overcurrent_threshold
                        )
                  )
                  OR EXISTS (
                      SELECT 1
                      FROM sensor_frame start_sf
                      WHERE start_sf.panel_id = p.panel_id
                        AND start_sf.received_at &lt;= #{thresholdAt}
                        AND start_sf.gas_raw &gt;= p.gas_threshold
                        AND NOT EXISTS (
                            SELECT 1
                            FROM sensor_frame recover_sf
                            WHERE recover_sf.panel_id = p.panel_id
                              AND recover_sf.received_at &gt; start_sf.received_at
                              AND recover_sf.gas_raw &lt; p.gas_threshold
                        )
                  )
                  OR EXISTS (
                      SELECT 1
                      FROM sensor_frame start_sf
                      WHERE start_sf.panel_id = p.panel_id
                        AND start_sf.received_at &lt;= #{thresholdAt}
                        AND start_sf.fire_raw &gt;= p.fire_threshold
                        AND NOT EXISTS (
                            SELECT 1
                            FROM sensor_frame recover_sf
                            WHERE recover_sf.panel_id = p.panel_id
                              AND recover_sf.received_at &gt; start_sf.received_at
                              AND recover_sf.fire_raw &lt; p.fire_threshold
                        )
                  )
              )
        )
    </select>

    <select id="findSustainedThresholdCautionTypes" resultType="string">
        SELECT 'LEAKAGE'
        FROM panel p
        WHERE p.panel_id = #{panelId}
          AND p.deleted_at IS NULL
          AND EXISTS (
              SELECT 1
              FROM sensor_frame start_sf
              WHERE start_sf.panel_id = p.panel_id
                AND start_sf.received_at &lt;= #{thresholdAt}
                AND start_sf.leak_ma &gt;= p.leak_ma_threshold
                AND NOT EXISTS (
                    SELECT 1
                    FROM sensor_frame recover_sf
                    WHERE recover_sf.panel_id = p.panel_id
                      AND recover_sf.received_at &gt; start_sf.received_at
                      AND recover_sf.leak_ma &lt; p.leak_ma_threshold
                )
          )
        UNION
        SELECT 'OVERHEAT'
        FROM panel p
        WHERE p.panel_id = #{panelId}
          AND p.deleted_at IS NULL
          AND EXISTS (
              SELECT 1
              FROM sensor_frame start_sf
              WHERE start_sf.panel_id = p.panel_id
                AND start_sf.received_at &lt;= #{thresholdAt}
                AND start_sf.temperature &gt;= p.temp_threshold
                AND NOT EXISTS (
                    SELECT 1
                    FROM sensor_frame recover_sf
                    WHERE recover_sf.panel_id = p.panel_id
                      AND recover_sf.received_at &gt; start_sf.received_at
                      AND recover_sf.temperature &lt; p.temp_threshold
                )
          )
        UNION
        SELECT 'HUMIDITY'
        FROM panel p
        WHERE p.panel_id = #{panelId}
          AND p.deleted_at IS NULL
          AND EXISTS (
              SELECT 1
              FROM sensor_frame start_sf
              WHERE start_sf.panel_id = p.panel_id
                AND start_sf.received_at &lt;= #{thresholdAt}
                AND start_sf.humidity &gt;= p.humidity_threshold
                AND NOT EXISTS (
                    SELECT 1
                    FROM sensor_frame recover_sf
                    WHERE recover_sf.panel_id = p.panel_id
                      AND recover_sf.received_at &gt; start_sf.received_at
                      AND recover_sf.humidity &lt; p.humidity_threshold
                )
          )
        UNION
        SELECT 'OVERCURRENT'
        FROM panel p
        WHERE p.panel_id = #{panelId}
          AND p.deleted_at IS NULL
          AND EXISTS (
              SELECT 1
              FROM sensor_frame start_sf
              WHERE start_sf.panel_id = p.panel_id
                AND start_sf.received_at &lt;= #{thresholdAt}
                AND start_sf.total_current &gt;= p.overcurrent_threshold
                AND NOT EXISTS (
                    SELECT 1
                    FROM sensor_frame recover_sf
                    WHERE recover_sf.panel_id = p.panel_id
                      AND recover_sf.received_at &gt; start_sf.received_at
                      AND recover_sf.total_current &lt; p.overcurrent_threshold
                )
          )
        UNION
        SELECT 'GAS'
        FROM panel p
        WHERE p.panel_id = #{panelId}
          AND p.deleted_at IS NULL
          AND EXISTS (
              SELECT 1
              FROM sensor_frame start_sf
              WHERE start_sf.panel_id = p.panel_id
                AND start_sf.received_at &lt;= #{thresholdAt}
                AND start_sf.gas_raw &gt;= p.gas_threshold
                AND NOT EXISTS (
                    SELECT 1
                    FROM sensor_frame recover_sf
                    WHERE recover_sf.panel_id = p.panel_id
                      AND recover_sf.received_at &gt; start_sf.received_at
                      AND recover_sf.gas_raw &lt; p.gas_threshold
                )
          )
        UNION
        SELECT 'FIRE'
        FROM panel p
        WHERE p.panel_id = #{panelId}
          AND p.deleted_at IS NULL
          AND EXISTS (
              SELECT 1
              FROM sensor_frame start_sf
              WHERE start_sf.panel_id = p.panel_id
                AND start_sf.received_at &lt;= #{thresholdAt}
                AND start_sf.fire_raw &gt;= p.fire_threshold
                AND NOT EXISTS (
                    SELECT 1
                    FROM sensor_frame recover_sf
                    WHERE recover_sf.panel_id = p.panel_id
                      AND recover_sf.received_at &gt; start_sf.received_at
                      AND recover_sf.fire_raw &lt; p.fire_threshold
                )
          )
    </select>

    <!-- 집계 결과를 분전반 현재 상태로 저장 -->
    <update id="updatePanelStatus">
        UPDATE panel
        SET status = #{status},
            updated_at = CURRENT_TIMESTAMP
        WHERE panel_id = #{panelId}
          AND deleted_at IS NULL
    </update>
</mapper>
